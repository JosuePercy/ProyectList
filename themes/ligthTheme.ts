import { createTheme } from "@nextui-org/react";

export const ligthTheme = createTheme({
  type: "light",
  theme: {
    colors: {
      primary: "#0F60AD", // color principal
      background: "#F5F5F5", // fondo
      card: "#FFFFFF", // color de tarjeta
      cardText: "#333333", // color de texto en tarjetas
      border: "#E2E2E2", // color del borde
    },
  },
});
